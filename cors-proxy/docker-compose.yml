version: '3.8'

services:
  cors-proxy:
    build: .
    image: applyo-cors-proxy:latest
    container_name: applyo-cors-proxy
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
      - PROXY_TOKEN=mein-geheimer-proxy-token
    restart: unless-stopped
